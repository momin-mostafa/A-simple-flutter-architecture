name: retrofit_flutter_tryout
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  retrofit: ^4.4.2
  dio: ^5.8.0+1
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator: ^9.1.9

  flutter_lints: ^5.0.0
  build_runner: ^2.4.15

flutter:
  uses-material-design: true
